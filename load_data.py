import pandas as pd
import os

# Define the folder paths
folder_paths = [
    '',
    '',
    ''
]
import csv

with open('', 'r') as file:
    reader = csv.reader(file)
    for i, row in enumerate(reader):
        if i >= 10: 
            break
        print(row)

with open('', 'r') as file:
    reader = csv.reader(file)
    for i, row in enumerate(reader):
        if i >= 10: 
            break
        print(row)

with open('', 'r') as file:
    reader = csv.reader(file)
    for i, row in enumerate(reader):
        if i >= 10: 
            break
        print(row)